<h3 class="h3__title"> Maths </h3>

<section class="example__section">
	<h5>Define a variable whose value is the result of the function that returns an absolute value.</h5>
	<pre>
function absFunction ($myEntry) {
	echo abs($myEntry);
	return;
}
absFunction(-4);
	</pre>
	<p>Result</p>
	<?php
	// echo abs(-4.2); // 4.2 (double/float)
	function absFunction($myEntry)
	{
		echo abs($myEntry);
		return;
	}
	absFunction(-4);
	?>
</section>

<section class="example__section">
	<h5>Define a variable whose value is the result of the function that returns a rounded value to the next highest integer.</h5>
	<pre>

	</pre>
	<p>Result</p>
	<?php
	?>
</section>

<section class="example__section">
	<h5>Define a variable whose value is the result of the function that returns the highest value of a series of values ​​that are received by parameter.</h5>
	<pre>

	</pre>
	<p>Result</p>
	<?php
	?>
</section>

<section class="example__section">
	<h5>Define a variable whose value is the result of the function that returns the lowest value of a series of values ​​that are received by parameter.</h5>
	<pre>

	</pre>
	<p>Result</p>
	<?php
	?>
</section>

<section class="example__section">
	<h5>Define a variable whose value is the result of the function that returns a random number</h5>
	<pre>

	</pre>
	<p>Result</p>
	<?php
	?>
</section>