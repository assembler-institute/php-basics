<code>
    <div style="margin-bottom:15px">
        <b>Define a variable whose value is the result of the function that returns an absolute value.
</b><br />
        <pre>
$absolute = abs(-4);
        </pre>
        <u>Result:</u>
        <i>
        <?php 
echo $absolute = abs(-4);
        ?>        
        </i>
    </div>

    <div style="margin-bottom:15px">
        <b>Define a variable whose value is the result of the function that returns a rounded value to the next highest integer.
</b><br />
        <pre>
$roundMaxValue = ceil(3.45);
        </pre>
        <u>Result:</u>
        <i>
        <?php
echo $roundMaxValue = ceil(3.45);
        ?>        
        </i>
    </div>

    <div style="margin-bottom:15px">
        <b>Define a variable whose value is the result of the function that returns the highest value of a series of values ​​that are received by parameter.
</b><br />
        <pre>
$maxValue = max(2, 3, 1, 25);
        </pre>
        <u>Result:</u>
        <i>
        <?php
echo $maxValue = max(2, 3, 1, 25);
        ?>        
        </i>
    </div>

    <div style="margin-bottom:15px">
        <b>Define a variable whose value is the result of the function that returns the lowest value of a series of values ​​that are received by parameter.</b><br />
        <pre>
$minValue = min(2, 3, 1, 25);
        </pre>
        <u>Result:</u>
        <i>
        <?php 
echo $minValue = min(2, 3, 1, 25);
        ?>        
        </i>
    </div>

    <div style="margin-bottom:15px">
        <b>Define a variable whose value is the result of the function that returns a random number</b><br />
        <pre>
$randomValue = rand();
        </pre>
        <u>Result:</u>
        <i>
        <?php 
echo $randomValue = rand();
        ?>        
        </i>
    </div>

</code>